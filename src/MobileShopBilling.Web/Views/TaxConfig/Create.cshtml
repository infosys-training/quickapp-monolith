@model MobileShopBilling.Application.DTOs.CreateTaxConfigurationDto
@{ ViewData["Title"] = "Add Tax Configuration"; }

<h4 class="mb-4"><i class="bi bi-plus-circle me-2"></i>Add Tax Configuration</h4>

<div class="card" style="max-width:500px;">
    <div class="card-body">
        <form asp-action="Create" method="post">
            <div class="mb-3">
                <label class="form-label">Tax Name *</label>
                <input asp-for="TaxName" class="form-control" placeholder="e.g., GST 18%" required />
            </div>
            <div class="mb-3">
                <label class="form-label">Rate (%) *</label>
                <input asp-for="Rate" type="number" step="0.01" class="form-control" required />
            </div>
            <div class="form-check mb-3">
                <input asp-for="IsDefault" class="form-check-input" />
                <label class="form-check-label">Set as default tax</label>
            </div>
            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-primary"><i class="bi bi-check-lg"></i> Save</button>
                <a href="/TaxConfig" class="btn btn-outline-secondary">Cancel</a>
            </div>
        </form>
    </div>
</div>
